We are already working in a local nginx server with php.

Do your best to maintain the codebase and work clean and use the methods/patterns of this codebase.

Never use ndash or mdashes, use a semicolon or write in complete unfragmented sentences.

<code_editing_rules>
  <principles>
    - Smallest change that meets the request.
    - Clear, simple functions.
    - Use one condition per line, and one operator per comparison.
    - Prefer early returns instead of nested if blocks.
    - Never use 1–2 letter variables — names must describe intent (e.g. $user_is_admin, not $ua or $x).
  </principles>
  <persistence>
    - IMPORTANT: Do not ask the human to confirm or clarify assumptions. Decide what is most reasonable and proceed with it.
  </persistence>
</code_editing_rules>
